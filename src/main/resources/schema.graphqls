type Query {
    myMaze(mazeId: ID!): Maze
}

type Mutation {
    createAMaze(mazeRunner: String!): ID!
}

type Subscription {
    tellMeSeconds: Int!
}

type Maze {
    yourPosition: Position!
}

type Position {
    x: Int!
    y: Int!
}
